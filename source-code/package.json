{
  "name": "doc-ocr-app",
  "version": "1.0.0",
  "description": "Document OCR and data extractor",
  "main": "server.js",
  "scripts": {
    "build": "cd my-react-app && npm install && npm run build",
    "start": "cd backend && node server.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "pdf-poppler": "^0.2.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
